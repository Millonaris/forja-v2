# Resultado de la compilación: se reconstruye con `npm run construir` y se
# publica en la rama gh-pages, no se versiona aquí.
prototipo/

# Iconos generados: salen de `npm run iconos`.
iconos/

node_modules/
.DS_Store
